<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "SA-TSI" %></title>
      <%= stylesheet_link_tag    "bootstrap", :media => "all" %>
      <%= javascript_include_tag "bootstrap" %>
      <%= stylesheet_link_tag "aplication", :media => "all" %>
      <%= javascript_include_tag :defaults %>
      <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <body>
    <div class="navbar navbar-inverse">
        <div class="navbar-inner">
            <a class="brand"><h2>SA-TSI</h2></a>
            <div class="container">
            <ul class="nav">
          <!--<li class="active"><a href="home_usuario.php">Home</a></li>
          <li><a href="videos_usuario.php">Videos</a></li>-->
        </ul>
            <div class="nav-collapse collapse">
            <form class="navbar-search pull-right" method="post"><br>
                <input type="text" class="search-query" placeholder="Buscar" name="buscar"><br>
                <% if current_user%>
                <div align="right">
                  <%= link_to "Cerrar sesión", logout_path %> <%= link_to 'Editar perfil', :controller => :users, :action => "edit", :id => current_user.id %>
              <% else %>
                <%= link_to "Regístrate", signup_path %> o
                <%= link_to "Inicia sesión", login_path %>
              <% end %>
            </form>
            </div>
        </div>
        </div>
    </div>
    <div class="hero-unit">
    <div id="container">
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
      <%= content_tag :h1, yield(:title) if show_title? %>
      <%= yield %>
    </div>
  </div>
   <div id="footer">
      <div class="container">
        <p class="muted credit">Designed by <a href="http://www.facebook.com/andres.jordan.zeballos">Andres Jordan</a>.</p>
      </div>
    </div>
  </body>
</html>
